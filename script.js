// Add JavaScript code for functionality and interactions
const form = document.querySelector('form');

form.addEventListener('submit', (e) => {
  e.preventDefault();
  console.log('Form submitted!');
  // Add form submission logic here
});
feather.replace()
